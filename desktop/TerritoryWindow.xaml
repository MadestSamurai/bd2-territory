<Window x:Class="BD2Territory.Desktop.TerritoryWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="{DynamicResource Ui28}" Width="900" Height="760" MinWidth="720" MinHeight="610"
 Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}"
 FontFamily="Microsoft YaHei UI" FontSize="13" WindowStartupLocation="CenterScreen">
 <Window.Resources>
  <Style TargetType="TextBox"><Setter Property="Padding" Value="8,5"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="BorderBrush" Value="#C9D0DA"/><Setter Property="BorderThickness" Value="1"/></Style>
  <Style TargetType="CheckBox"><Setter Property="Margin" Value="0,0,28,0"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="FontSize" Value="14"/></Style>
 </Window.Resources>
 <Grid Margin="24,20">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,18">
   <ComboBox x:Name="LanguageBox" DockPanel.Dock="Right" Width="110" MinHeight="30" VerticalAlignment="Center" Margin="12,0,0,0" SelectionChanged="LanguageChanged" AutomationProperties.Name="Language"><ComboBoxItem Tag="zh-CN" Content="简体中文"/><ComboBoxItem Tag="en-US" Content="English"/></ComboBox>
   <Button DockPanel.Dock="Right" Content="{DynamicResource Ui29}" Click="LayoutClick" Margin="12,0,0,0"/>
   <TextBlock DockPanel.Dock="Right" x:Name="VersionText" Text="{DynamicResource Ui30}" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
   <StackPanel><TextBlock Text="{DynamicResource Ui31}" FontSize="24" FontWeight="SemiBold"/><TextBlock Text="{DynamicResource Ui32}" Margin="0,4,0,0" Foreground="{StaticResource MutedBrush}"/></StackPanel>
  </DockPanel>
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,8,0">
   <StackPanel>
    <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,14">
     <StackPanel>
      <TextBlock x:Name="StatusText" Text="{DynamicResource Ui33}" FontSize="18" FontWeight="SemiBold"/>
      <TextBlock x:Name="ReasonText" Text="{DynamicResource Ui34}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0"/>
      <TextBlock x:Name="TargetText" TextWrapping="Wrap" Margin="0,8,0,0"/>
      <TextBlock x:Name="ErrorText" TextWrapping="Wrap" Foreground="#A4262C" Margin="0,8,0,0" Visibility="Collapsed"/>
     </StackPanel>
    </Border>
    <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,14">
     <StackPanel>
      <TextBlock Text="{DynamicResource Ui35}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,14"/>
      <WrapPanel Margin="0,0,0,18"><CheckBox x:Name="LoggingBox" Content="{DynamicResource Ui36}" Checked="SettingsChanged" Unchecked="SettingsChanged"/><CheckBox x:Name="MiningBox" Content="{DynamicResource Ui37}" Checked="SettingsChanged" Unchecked="SettingsChanged"/><CheckBox x:Name="FarmingBox" Content="{DynamicResource Ui38}" Checked="SettingsChanged" Unchecked="SettingsChanged"/></WrapPanel>
      <WrapPanel Margin="0,0,0,18"><CheckBox x:Name="VehicleBox" Content="{DynamicResource Ui39}" Checked="SettingsChanged" Unchecked="SettingsChanged" ToolTip="{DynamicResource Ui40}"/><CheckBox x:Name="DashBox" Content="{DynamicResource Ui41}" Checked="SettingsChanged" Unchecked="SettingsChanged" ToolTip="{DynamicResource Ui42}"/></WrapPanel>
      <CheckBox x:Name="NavMeshBox" Content="{DynamicResource NavMeshLabel}" ToolTip="{DynamicResource NavMeshHint}" Margin="0,0,0,6" Checked="SettingsChanged" Unchecked="SettingsChanged"/>
      <TextBlock Text="{DynamicResource NavMeshHint}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,0,0,18"/>
      <WrapPanel>
       <StackPanel Margin="0,0,28,8"><TextBlock Text="{DynamicResource Ui43}" Margin="0,0,0,6"/><TextBox x:Name="IntervalBox" Width="150" HorizontalAlignment="Left" LostFocus="SettingsChanged" AutomationProperties.Name="{DynamicResource Ui44}"/></StackPanel>
       <StackPanel Margin="0,0,0,8"><TextBlock Text="{DynamicResource Ui45}" Margin="0,0,0,6"/><TextBox x:Name="BudgetBox" Width="180" HorizontalAlignment="Left" LostFocus="SettingsChanged" AutomationProperties.Name="{DynamicResource Ui46}"/></StackPanel>
      </WrapPanel>
      <TextBlock x:Name="SettingsHint" Text="{DynamicResource Ui47}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,3,0,0"/>
     </StackPanel>
    </Border>
    <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,14">
     <StackPanel>
      <TextBlock Text="{DynamicResource Ui48}" FontSize="15" FontWeight="SemiBold"/>
      <ComboBox x:Name="PlantingModeBox" SelectedIndex="0" SelectionChanged="PlantingChanged" Margin="0,10,0,8" MinHeight="34" AutomationProperties.Name="{DynamicResource PlantingModeLabel}">
       <ComboBoxItem Content="{DynamicResource RecipeModeLabel}"/><ComboBoxItem Content="{DynamicResource FixedModeLabel}"/>
      </ComboBox>
      <StackPanel x:Name="FixedCropPanel" Visibility="Collapsed" Margin="0,0,0,12">
       <TextBlock Text="{DynamicResource FixedSeedLabel}" Margin="0,0,0,6"/>
       <ComboBox x:Name="FixedSeedBox" SelectedValuePath="Id" SelectionChanged="PlantingChanged" MinHeight="34" AutomationProperties.Name="{DynamicResource FixedSeedLabel}">
        <ComboBox.ItemTemplate><DataTemplate><StackPanel><TextBlock Text="{Binding Name}"/><TextBlock Text="{Binding Reason}" TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource MutedBrush}"/></StackPanel></DataTemplate></ComboBox.ItemTemplate>
        <ComboBox.ItemContainerStyle><Style TargetType="ComboBoxItem"><Setter Property="IsEnabled" Value="{Binding Available}"/></Style></ComboBox.ItemContainerStyle>
       </ComboBox>
       <TextBlock x:Name="FixedCropHint" Text="{DynamicResource FixedCropHintLabel}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0"/>
      </StackPanel>
      <StackPanel x:Name="RecipePanel">
      <TextBlock Text="{DynamicResource RecipeLabel}" Margin="0,4,0,0"/>
      <ComboBox x:Name="RecipeBox" SelectedValuePath="Id" SelectionChanged="RecipeChanged" Margin="0,10,0,0" MinHeight="34" AutomationProperties.Name="{DynamicResource Ui49}"><ComboBox.ItemTemplate><DataTemplate><StackPanel MaxWidth="600"><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Text="{Binding Ratio}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="12"/><TextBlock Text="{Binding Reason}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="12"/></StackPanel></DataTemplate></ComboBox.ItemTemplate><ComboBox.ItemContainerStyle><Style TargetType="ComboBoxItem"><Setter Property="IsEnabled" Value="{Binding Available}"/></Style></ComboBox.ItemContainerStyle></ComboBox>
      <TextBlock x:Name="RecipeHint" Text="{DynamicResource Ui50}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,12"/>
      </StackPanel>
      <WrapPanel Margin="0,0,0,10"><CheckBox x:Name="CookingBox" Content="{DynamicResource CookingLabel}" Checked="SettingsChanged" Unchecked="SettingsChanged"/><TextBlock Text="{DynamicResource CookingBatchLabel}" VerticalAlignment="Center" Margin="0,0,8,0"/><TextBox x:Name="CookingBatchBox" Width="80" LostFocus="SettingsChanged" AutomationProperties.Name="Cooking batch size"/></WrapPanel>
      <TextBlock Text="{DynamicResource CookingHint}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,8"/>
      <TextBlock x:Name="CookingText" TextWrapping="Wrap" Margin="0,0,0,8"/>
      <TextBlock x:Name="BatchText" Text="{DynamicResource Ui51}" Margin="0,0,0,6"/>
      <ProgressBar x:Name="BatchBar" Height="6" Maximum="100" Foreground="{StaticResource PrimaryBrush}" Background="#E8EDF3" BorderThickness="0"/>
      <DataGrid x:Name="CropsGrid" Margin="0,12,0,0" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserResizeRows="False" CanUserSortColumns="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF0F4" BorderThickness="0" RowHeight="34" ColumnHeaderHeight="32" Background="White" SelectionMode="Single" MinHeight="100">
       <DataGrid.Columns><DataGridTextColumn Header="{DynamicResource Ui52}" Binding="{Binding Name}" Width="*"/><DataGridTextColumn Header="{DynamicResource Ui53}" Binding="{Binding Required}" Width="84"/><DataGridTextColumn Header="{DynamicResource Ui54}" Binding="{Binding Inventory}" Width="84"/><DataGridTextColumn Header="{DynamicResource Ui55}" Binding="{Binding Growing}" Width="94"/><DataGridTextColumn Header="{DynamicResource Ui56}" Binding="{Binding Growth}" Width="100"/></DataGrid.Columns>
      </DataGrid>
      <TextBlock x:Name="StatsText" Text="{DynamicResource Ui57}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,10,0,0"/>
     </StackPanel>
    </Border>
    <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,14">
     <StackPanel>
      <TextBlock Text="{DynamicResource SalesTitle}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12"/>
      <WrapPanel><CheckBox x:Name="AutoSellBox" Content="{DynamicResource SalesEnabled}" Checked="SettingsChanged" Unchecked="SettingsChanged"/><TextBlock Text="{DynamicResource SalesRetain}" VerticalAlignment="Center" Margin="0,0,8,0"/><TextBox x:Name="SellThresholdBox" Width="84" LostFocus="SettingsChanged" AutomationProperties.Name="{DynamicResource SalesRetain}"/></WrapPanel>
      <TextBlock Text="{DynamicResource SalesHelp}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,0"/>
      <TextBlock x:Name="SalesText" TextWrapping="Wrap" Margin="0,8,0,0"/>
     </StackPanel>
    </Border>
    <Expander x:Name="DiagnosticsPanel" Header="{DynamicResource Ui58}" Margin="0,0,0,12" Expanded="DiagnosticsExpanded">
     <StackPanel Margin="0,12,0,0">
      <TextBlock Text="{DynamicResource Ui59}" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
      <TextBox x:Name="DataPathBox" IsReadOnly="True" TextWrapping="Wrap" AutomationProperties.Name="{DynamicResource Ui60}"/>
      <TextBox x:Name="DiagnosticsBox" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="180" Margin="0,8,0,0" AutomationProperties.Name="{DynamicResource Ui61}"/>
     </StackPanel>
    </Expander>
   </StackPanel>
  </ScrollViewer>
  <DockPanel Grid.Row="2" Margin="0,14,0,0" LastChildFill="True">
   <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button x:Name="StartButton" Content="{DynamicResource Ui62}" Style="{StaticResource PrimaryButton}" Click="StartClick" IsEnabled="False"/><Button x:Name="StopButton" Content="{DynamicResource Ui63}" Click="StopClick" IsEnabled="False" Margin="0"/></StackPanel>
   <Button x:Name="ConnectButton" Content="{DynamicResource Ui64}" Click="ConnectClick" HorizontalAlignment="Left"/>
  </DockPanel>
 </Grid>
</Window>
