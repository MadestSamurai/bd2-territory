<Window x:Class="BD2Territory.Desktop.LayoutWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="{DynamicResource Ui1}" Width="1050" Height="820" MinWidth="780" MinHeight="640" FontFamily="Microsoft YaHei UI" FontSize="13"
 Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}" WindowStartupLocation="CenterOwner">
 <Grid Margin="24,20"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <StackPanel Margin="0,0,0,16"><TextBlock Text="{DynamicResource Ui2}" FontSize="24" FontWeight="SemiBold"/><TextBlock Text="{DynamicResource Ui3}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,6,0,0"/></StackPanel>
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
   <StackPanel>
    <WrapPanel Margin="0,0,0,12"><Button x:Name="LoadButton" Content="{DynamicResource Ui4}" Click="LoadClick"/><Button x:Name="ExportCurrentButton" Content="{DynamicResource Ui5}" Click="ExportCurrentClick"/><Button Content="{DynamicResource Ui6}" Click="SaveClick"/></WrapPanel>
    <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,14"><StackPanel>
     <WrapPanel><StackPanel Margin="0,0,16,10"><TextBlock Text="{DynamicResource Ui7}" Margin="0,0,0,6"/><ComboBox x:Name="ChunkBox" Width="240" MinHeight="34" DisplayMemberPath="Label" SelectedValuePath="Id" SelectionChanged="ChunkChanged" AutomationProperties.Name="{DynamicResource Ui8}"/></StackPanel>
      <StackPanel Margin="0,0,16,10"><TextBlock Text="{DynamicResource Ui9}" Margin="0,0,0,6"/><ComboBox x:Name="TemplateBox" Width="220" MinHeight="34" SelectedIndex="0"><ComboBoxItem Content="{DynamicResource Ui10}"/><ComboBoxItem Content="{DynamicResource Ui11}"/><ComboBoxItem Content="{DynamicResource Ui12}"/></ComboBox></StackPanel>
      <Button x:Name="TemplateButton" Content="{DynamicResource Ui13}" Click="TemplateClick" VerticalAlignment="Bottom" Margin="0,0,0,10"/></WrapPanel>
     <TextBlock x:Name="TitleText" Text="{DynamicResource Ui14}" FontSize="16" FontWeight="SemiBold" Margin="0,6,0,10"/>
     <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Viewbox Stretch="Uniform" MaxHeight="220" HorizontalAlignment="Left"><Canvas x:Name="Board" Width="320" Height="320" Background="#F4F6F9"/></Viewbox>
      <StackPanel Grid.Column="1" Margin="18,0,0,0"><TextBlock Text="{DynamicResource Ui15}" TextWrapping="Wrap"/><TextBlock Text="{DynamicResource Ui16}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,12"/>
       <TextBlock x:Name="SummaryText" Text="{DynamicResource Ui17}" TextWrapping="Wrap"/><TextBlock x:Name="AccountText" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,10,0,0"/></StackPanel>
     </Grid>
    </StackPanel></Border>
    <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,14"><StackPanel>
     <TextBlock Text="{DynamicResource Ui18}" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="{DynamicResource Ui19}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,6,0,10"/>
     <DataGrid x:Name="CostGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" RowHeight="32" ColumnHeaderHeight="32" MinHeight="65" MaxHeight="200" BorderThickness="0" Background="White">
      <DataGrid.Columns><DataGridTextColumn Header="{DynamicResource Ui20}" Binding="{Binding Name}" Width="*"/><DataGridTextColumn Header="{DynamicResource Ui21}" Binding="{Binding Count}" Width="100"/><DataGridTextColumn Header="{DynamicResource Ui22}" Binding="{Binding Owned}" Width="100"/><DataGridTextColumn Header="{DynamicResource Ui23}" Binding="{Binding Missing}" Width="100"/></DataGrid.Columns>
     </DataGrid>
    </StackPanel></Border>
   </StackPanel>
  </ScrollViewer>
  <StackPanel Grid.Row="2" Margin="0,10,0,0"><TextBlock x:Name="TotalText" Text="" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,0,0,6"/><TextBlock x:Name="MessageText" Text="{DynamicResource Ui24}" TextWrapping="Wrap" Margin="0,0,0,8"/><ProgressBar x:Name="Progress" Height="5" Margin="0,0,0,12"/>
   <DockPanel><Button x:Name="PauseButton" DockPanel.Dock="Right" Content="{DynamicResource Ui25}" Click="PauseClick" IsEnabled="False" Margin="8,0,0,0"/><Button x:Name="ApplyButton" DockPanel.Dock="Right" Content="{DynamicResource Ui26}" Style="{StaticResource PrimaryButton}" Click="ApplyClick" IsEnabled="False" Margin="8,0,0,0"/><Button x:Name="PreviewButton" Content="{DynamicResource Ui27}" Click="PreviewClick" HorizontalAlignment="Left"/></DockPanel>
  </StackPanel>
 </Grid>
</Window>
